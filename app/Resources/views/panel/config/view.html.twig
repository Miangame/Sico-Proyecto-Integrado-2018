{% extends 'panel/base.html.twig' %}
{% block title %}SICO: Config. admin{% endblock %}
{% block body %}
    <h3 class="col s12">Configuración</h3>
    <div class="row">
        <div class="col s12 p-0 br-1">
            <h4>Nombre del insituto/departamento</h4>
            {{ form_start(formConfig, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(formConfig) }}
            {{ form_end(formConfig) }}
        </div>
    </div>

{% endblock %}